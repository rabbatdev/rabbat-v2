// rabbat/plugin → the @rabbat/vite backend plugin (framework-agnostic: backend +
// the Cloudflare runtime). The original name was `rabbatPlugin`.
export { rabbat as rabbatPlugin, rabbat, type RabbatOptions } from "@rabbat/vite"
